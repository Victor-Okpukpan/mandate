{
  "name": "@mandate/agent-adversary",
  "version": "0.0.0",
  "private": true,
  "description": "A red-team agent with raw tools and a prompt instructing it to escape its own mandate by any means. Deliberately on the batteries-included Claude Agent SDK, not the Tool Runner — see mandate.md §12.3.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.3.260",
    "@mandate/agents-shared": "workspace:*",
    "@mandate/shared": "workspace:*",
    "@privy-io/server-auth": "^1.32.5",
    "viem": "^2.56.3",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.23.13",
    "typescript": "^5.7.3"
  }
}
