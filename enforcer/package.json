{
  "name": "@mandate/enforcer",
  "version": "0.0.0",
  "private": true,
  "description": "Watches Sepolia mandate events and propagates them into a Privy conditional policy and a signed Arc anchor. A propagator, not an authority — it can only narrow a mandate or revoke it, never widen one.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "start": "tsx src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@mandate/shared": "workspace:*",
    "@privy-io/node": "0.34.0",
    "ethers": "^6.17.0",
    "viem": "^2.56.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.23.13",
    "typescript": "^5.7.3"
  }
}
